# Form Section Layout CSS Source

`form_section_layout.css` is the published assembled asset.

Source lives in this directory and is concatenated in the order declared by `manifest.json`.

Use:

```powershell
.\.venv\Scripts\python.exe scripts\build_form_section_layout_css.py
.\.venv\Scripts\python.exe scripts\check_form_section_layout_css_parity.py
```
